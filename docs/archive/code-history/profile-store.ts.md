# Archived `lib/profile-store.ts`

> 归档说明：本文件原位于 `lib/profile-store.ts`。经引用检查，当前没有任何运行路径 import 它；它只是早期单账号 localStorage 档案占位。当前 MVP 不靠浏览器 localStorage 保存账号事实，账号 JSON 放在 `data/accounts/`。

```ts
"use client";

// ⚠️ 已废弃（2026-06-10）：单账号档案已升级为多账号工作台，请用 lib/account-store.ts。
// 旧 key（yowow-adaptation.profile.v1）由 account-store 自动迁移，本文件仅保留占位防误用。

export {};
```
